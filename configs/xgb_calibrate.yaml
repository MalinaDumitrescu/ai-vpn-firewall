method: "platt"   # platt | isotonic
fallback_fit_splits: ["train"]  # used only when val has <2 classes

splits:
  train: "train"
  val: "val"
  test: "test"

inputs:
  preds_path: "artifacts/xgb/preds.parquet"

outputs:
  calibrator_path: "artifacts/xgb/calibrator.pkl"
  metrics_path: "artifacts/xgb/metrics_calibrated.json"
  preds_calibrated_path: "artifacts/xgb/preds_calibrated.parquet"
